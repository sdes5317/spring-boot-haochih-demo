server:
  port: 19100

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:coin-desk-db # h2 database
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
